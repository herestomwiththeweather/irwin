<div class="mt-4 px-5">
  <div class="flex justify-between items-center">
    <h1 class="font-bold text-4xl">Code of Conduct</h1>
  </div>
  <div id="rules" class="min-w-full mt-6">
    <% @rules.each do |rule| %>
      <%= render rule %>
      <hr class="my-4 w-1/4">
    <% end %>
  </div>
</div>
