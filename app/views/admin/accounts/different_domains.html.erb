<div class="mt-4 px-5">

  <div class="flex justify-between items-center">
    <h1 class="font-bold text-4xl">Different (<%= @admin_accounts.count %>)</h1>
  </div>
  <div id="admin_accounts" class="min-w-full">
    <%= render @admin_accounts %>
  </div>
  <%= paginate(@admin_accounts) %>
</div>
